export function ChannelCard({channel}:{channel:{name:string;initial:string;description:string;contents:string;url:string}}){return <article className="channel-card card"><div className="channel-logo">{channel.initial}</div><div><h3>{channel.name}</h3><p>{channel.description}</p><small>{channel.contents}</small><a href={channel.url} target="_blank" rel="noreferrer">YouTube 채널 방문 →</a></div></article>}
